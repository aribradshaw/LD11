# Simple .htaccess for HostGator
Options +Indexes
Allow from all

# Enable directory browsing
DirectoryIndex index.html index.php

# Allow access to subdirectories
<Directory "ld11">
    Options +Indexes
    Allow from all
</Directory>
